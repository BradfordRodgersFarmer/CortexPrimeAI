import {FieldValues, SubmitHandler, useForm, } from 'react-hook-form';
type FormValues = {
    playerName: string
    bookTitle: string
}
export const CoverPage = (storyCreator: any) => {

     return (


)
}